apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "apim-320-dist-oke-sc"
  resources:
    requests:
      storage: 1Gi