apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "apim-k8s-oke.resource.prefix" . }}-client-manage-pod-artifacts-storage-volume
  namespace : {{ .Release.Namespace }}
spec:
  capacity:
    storage: 15Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: {{ .Values.mountTargetIP }}
    path: "/{{ template "apim-k8s-oke.resource.prefix" . }}-client-volume/client-log-storage-volume"